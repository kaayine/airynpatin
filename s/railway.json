{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python SIA.py",
    "healthcheckPath": "/beranda",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}